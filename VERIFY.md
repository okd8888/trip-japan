# 驗證清單

裝好之後照這份走一遍，就知道每個功能是不是真的在動。
每一步都寫了**該看到什麼**，看到不一樣的地方直接跳到最後的「疑難排解」。

大部分步驟只要用瀏覽器，不用開終端機。

---

## 第 0 步：先確認 worker/ 在你要用的分支上

同步功能的程式碼在 `worker/` 資料夾。**如果它還沒合併進 `main`**，
底下的「Deploy to Cloudflare」按鈕會找不到檔案（按鈕的網址是指向 `main` 的）。

到 GitHub 打開你的 repo，切到 `main` 分支，看有沒有 `worker/` 資料夾：

- **有** → 直接往下做。
- **沒有** → 先把功能分支合併進 `main`（開 Pull Request 或 `git merge`），
  或是暫時用分支版的按鈕網址（把網址裡的 `main` 換成你的分支名）。

---

## 第 1 步：部署後端

三條路，選一條就好。**先試方法 A，失敗就換 B。**

### 方法 A：一鍵部署按鈕

打開 repo 裡的 `worker/README.md`，按 **Deploy to Cloudflare**。
登入 Cloudflare（免費帳號即可）後，它會在你的 GitHub 建一個新 repo，
並自動建好 Worker 與 D1 資料庫。

> ⚠️ Cloudflare 這個按鈕在「repo 子資料夾」的情況下有已知問題，
> 有時候會建出一個只有兩個檔案、Worker 停在 Hello World 的空專案。
> 如果部署完打開 `/api/health` 沒反應，或 Worker 內容是 Hello World，
> 就是踩到這個雷了，改用方法 B 或 C。

### 方法 B：用指令部署（最可靠）

```bash
cd worker
npm install
npm run deploy
```

wrangler 4 會偵測到 `wrangler.jsonc` 裡的 D1 還不存在，直接問你要不要建，
按 Enter 讓它建就好，**`database_id` 不用手動填**。

如果它沒問（wrangler 版本太舊），手動建一個再把印出來的 ID 填進 `d1_databases`：

```bash
npx wrangler d1 create trip-sync
```

本機開發：

```bash
npm run dev
```

### 方法 C：完全用網頁介面（不想碰終端機的話）

1. Cloudflare 後台 → **Workers & Pages** → **Create** → **Start from Hello World** → 命名 `trip-sync`
2. 進 Worker 的 **Edit code**，把 `worker/src/index.js` 全部內容貼上去，Deploy
3. 後台 → **Storage & Databases** → **D1** → **Create database**，命名 `trip-sync`
4. 回到 Worker → **Settings** → **Bindings** → **Add** → **D1 database**
   → Variable name 填 **`DB`**（一定要叫這個），選剛才建的資料庫 → Deploy

> 資料表不用手動建，Worker 第一次收到請求時會自己建好。

部署完你會拿到一個網址，像 `https://trip-sync.你的帳號.workers.dev`。
**把它記下來，後面每一步都要用。**

---

## 第 2 步：確認後端活著

瀏覽器網址列直接貼（把網址換成你自己的）：

```
https://trip-sync.你的帳號.workers.dev/api/health
```

**該看到：**

```json
{"ok":true}
```

這一步同時驗證了三件事：**Worker 活著**、**D1 綁對了**、**資料表自動建好了**
（資料表是第一次收到請求時建的，所以有回應就代表資料庫是通的）。

---

## 第 3 步：驗匯率端點

```
https://trip-sync.你的帳號.workers.dev/api/rate?from=JPY&to=TWD
```

**第一次該看到**（`rate` 是 0.2 上下的數字）：

```json
{"rate":0.2134,"source":"currency-api","updatedAt":1788160965260,"cached":false}
```

**重新整理再看一次**，`rate` 一樣，但：

```json
{"cached":true}
```

`cached` 從 `false` 變 `true`，就代表快取生效了——第二次沒有再去打上游。

> 如果第一次就回 `502`，表示 Cloudflare 那邊當下也抓不到上游匯率來源（少見）。
> 不影響使用，網站會自動往下一個來源試。過幾分鐘再試一次通常就好了。

---

## 第 4 步：接上網站

1. 電腦開你的行程網站 → 底部「**編輯**」分頁 → 拉到最下面「**跨裝置同步**」
2. **同步端點**填第 1 步拿到的網址
3. **你的顯示名稱**填一個名字（記帳會標記是誰記的）
4. 按「**建立同步行程**」

**該看到：**

- 右上角徽章從「未啟用」變成「**可編輯**」
- **行程碼**和**編輯金鑰**自動填好了
- 下方訊息顯示建立完成與行程碼

---

## 第 5 步：驗匯率排到第一順位

「**匯率**」分頁 → 按「**重新抓取匯率**」。

**該看到**下方那行寫著：

```
匯率來源：自己的匯率端點｜更新於 ...
```

寫「自己的匯率端點」就代表**第 0 順位生效了**——網站現在是先問你的 Worker，
問不到才往下試三個公開 API。這是出國網路差時匯率不會失準的關鍵。

接著隨手記一筆花費（「花費」分頁），等一下要用。

---

## 第 6 步：驗跨裝置分享（重點）

1. 電腦「編輯」分頁 → 按「**複製分享連結**」
2. 用 LINE 或任何方式把連結傳到**手機**
3. 手機打開那個連結

**該看到：**

- 手機顯示**同一份行程**
- 「花費」分頁裡有**你剛才在電腦記的那筆**
- 「編輯」分頁的徽章是「**唯讀**」
- 網址列的 `?trip=...` 參數**已經自動消失了**（避免連結被一直轉傳出去）

這就是給同行的人用的模式：看得到全部，改不了你的東西。

---

## 第 7 步：驗共同編輯

想讓手機也能記帳（例如另一半也要記）：

1. 從電腦的同步面板複製**編輯金鑰**，傳到手機
2. 手機「編輯」分頁 → 貼進**編輯金鑰**欄位 → 填自己的**顯示名稱** → 按「**連結現有行程**」
3. 手機徽章變成「**可編輯**」
4. 手機記一筆花費
5. 電腦按「**立刻同步**」

**該看到：** 電腦出現手機記的那筆，而且那筆標著**手機上填的名字**。

---

## 第 8 步：驗刪除不會復活（最容易出錯的地方）

兩台裝置各存一份資料時，「刪除」很容易在下次同步被對方的資料救回來。
這個專案是用墓碑機制處理的，驗一下：

1. **手機**上刪掉任一筆花費
2. **電腦**按「立刻同步」

**該看到：** 那筆在電腦上也消失了。

3. 再讓兩邊互相同步兩三次（電腦按「立刻同步」，手機也按）

**該看到：** 那筆**始終沒有回來**。

如果它復活了，就是墓碑沒同步出去，回報一下。

---

## 第 9 步：驗版本衝突提示

1. 電腦和手機**都**開著「編輯」分頁
2. 手機改一下行程名稱，等三秒（讓它送出去）
3. 電腦也改行程名稱

**該看到：** 電腦跳出「這份行程已被其他裝置更新」的確認框，
問你要載入對方的版本還是保留自己的。

按「確定」＝用對方的；按「取消」＝你的蓋過去。兩個選擇都不會讓資料靜悄悄消失。

---

## 第 10 步：驗「沒設定的人完全不受影響」

這一步是確認**別人 clone 這個專案時，不會被同步功能干擾**。

1. 開一個**無痕視窗**，打開你的網站（無痕沒有 localStorage，等於全新的使用者）

**該看到：**

- 網站正常顯示 `data/trip.js` 裡的行程
- 「編輯」分頁的同步徽章是「**未啟用**」
- 記帳、匯率、打包清單全部正常
- 完全不會連到任何 Worker

這就是設計上的重點：**沒填端點的人，行為和純靜態版本一模一樣。**

---

## 疑難排解

| 症狀 | 原因 | 怎麼辦 |
|---|---|---|
| `/api/health` 打不開 | Worker 沒部署成功 | 看第 1 步，改用方法 B 或 C |
| `/api/health` 回「Worker 還沒綁定 D1 資料庫」 | D1 綁定名稱不是 `DB` | 到 Worker → Settings → Bindings，把 Variable name 改成 `DB` |
| Worker 顯示 Hello World | 踩到一鍵部署按鈕的子資料夾問題 | 改用方法 B 或 C |
| 按鈕說 `Repository not found. Are you sure it's public?` | `main` 上沒有 `worker/`。訊息很誤導，跟公開與否無關 | 先把功能分支合併進 `main` |
| 按鈕說 `problem parsing the Wrangler configuration file` | 設定檔它讀不懂 | 確認是 `worker/wrangler.jsonc`，且 `d1_databases` 裡**沒有** `database_id` |
| 按「建立同步行程」說連不上 | 端點網址打錯，或結尾多了斜線以外的東西 | 先用第 2 步確認 `/api/health` 通得了 |
| 手機打開分享連結是空白行程 | 連結沒帶到 `api` 參數 | 重新用「複製分享連結」按鈕產生，不要手打 |
| 手機記帳同步不上去 | 手機是唯讀（沒有編輯金鑰） | 看第 7 步 |
| 匯率來源沒寫「自己的匯率端點」 | 端點沒填，或 `/api/rate` 回錯誤 | 先用第 3 步確認端點本身正常 |
| 刪掉的花費又跑回來 | 墓碑沒同步 | 這是 bug，請回報 |
| 範例行程顯示「已結束」 | 範例的 `startDate` 是 2027-04-02，過期了 | 換成你自己的行程日期就好 |

---

## 附：不用瀏覽器的快速檢查

有終端機的話（Windows 的 cmd 或 Git Bash 都可以）：

```bash
curl https://trip-sync.你的帳號.workers.dev/api/health
curl "https://trip-sync.你的帳號.workers.dev/api/rate?from=JPY&to=TWD"
```

`/api/health` 回 `{"ok":true}`、`/api/rate` 回一個 `rate` 數字，後端就沒問題了。
